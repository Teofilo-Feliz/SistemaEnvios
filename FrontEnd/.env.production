# Produccion. Mismo origen que QA; lo unico que cambia es el emisor de los tokens, que debe
# coincidir con el de appsettings.Production.json.
VITE_API_BASE_URL=/api
VITE_AUTH_AUTHORITY=https://authserver.rehabilitacion.org.do
VITE_AUTH_CLIENT_ID=logi-track-app
VITE_AUTH_RESPONSE_TYPE=code
# El backend (HttpUserContext) espera los claims roles, permissions, affiliate y position.
VITE_AUTH_SCOPES=openid profile roles
